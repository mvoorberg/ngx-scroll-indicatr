import { NgxScrollIndicatrDirective } from './ngx-scroll-indicatr.directive';

describe('NgxScrollIndicatrDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxScrollIndicatrDirective();
    expect(directive).toBeTruthy();
  });
});
